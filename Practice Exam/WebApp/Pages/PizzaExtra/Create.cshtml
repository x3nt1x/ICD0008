@page
@model WebApp.Pages_PizzaExtra.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PizzaExtra</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PizzaExtra.PizzaId" class="control-label"></label>
                <select asp-for="PizzaExtra.PizzaId" class ="form-control" asp-items="ViewBag.PizzaId"></select>
            </div>
            <div class="form-group">
                <label asp-for="PizzaExtra.ExtraId" class="control-label"></label>
                <select asp-for="PizzaExtra.ExtraId" class ="form-control" asp-items="ViewBag.ExtraId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
